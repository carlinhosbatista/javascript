function count() {
    let exit.document.getElementById('exit')
    let exit = document.getElementById('exit').style.fontSize = '20px'
    let c = 1
    exit.innerHTML = '<h2>Contando de 1 até 10</h2>'
    while (c <= 10) {
            exit.innerHTML += ` ${c} \u{1f449}`
        c++
    }
}